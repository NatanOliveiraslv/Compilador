a = 3.5;
b = a + 5;
c = (b - 2) * 2;

tentaisso(falsidade)[
    joganatela("Ta no if:");
    joganatela(c);
]senaoderfazisso[
    joganatela("Ta no else:");
    joganatela(a);
]

repetidor(a)[
    joganatela("Ta no for");
]

repetidor(6)[
    a = a + 1;
    joganatela("Ta no for");
]